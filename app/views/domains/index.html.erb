<%= form_tag domains_path, :method => 'get' do %>
  <%= render "layouts/search", params: params %>
<% end %>
<table>
<% @domains.each do |domain| %>
<tr>
    <td><%= domain.created_at %></td>
    <td><%= link_to domain.url, domain_path(domain) %></td>
</tr>
<% end %>
</table>