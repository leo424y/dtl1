<%= tw_time(@node.created_at) %>

<table>
<% @node.posts.each do |post| %>
<tr>
    <td><%= link_to(post.date, post.url, target: "_blank") %></td>
    <td><%= post.title %></td>
</tr>
<% end %>
</table>
