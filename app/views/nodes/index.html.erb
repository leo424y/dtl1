<table>
<% @nodes.each do |node| %>
<tr>
    <td><%= link_to node.archive['user_name'] || node.archive['name'], node.url %></td>
    <td><%= node.posts.count %></td>
    <td><%= node.posts.average(:score) %></td>
    <td><%= node.archive['source'] %></td>
</tr>
<% end %>
</table>